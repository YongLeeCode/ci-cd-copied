name: Trigger sync

on:
  repository_dispatch:
    types: [main_updated]

jobs:
  trigger-sync:
    runs-on: ubuntu-latest
    if: github.repository == 'YongLeeCode/ci-cd-copied'
    steps:
      - run: echo 'sync'
